const HOTEL_PHONE_INTERNATIONAL = '94777778744';
const HOTEL_EMAIL = 'canbohotel@gmail.com';
export function initFormValidation() { initContactForm(); initAvailabilityForm(); setMinimumDates(); updateCurrentYear(); }
function initContactForm(){const form=document.getElementById('contact-form'); if(!form)return; form.addEventListener('submit',e=>{e.preventDefault(); let ok=true; clearValidation(form); const fields=[{name:'name',minLength:2,message:'Please enter your full name (min 2 characters)'},{name:'email',type:'email',message:'Please enter a valid email address'},{name:'phone',minLength:10,message:'Please enter a valid phone number (min 10 digits)'},{name:'inquiry',message:'Please select an inquiry type'},{name:'message',minLength:10,message:'Please enter a message (min 10 characters)'}]; fields.forEach(f=>{const input=form.querySelector(`[name="${f.name}"]`); if(!input)return; const v=input.value.trim(); let valid=!!v; if(valid&&f.type==='email')valid=/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v); if(valid&&f.minLength)valid=v.length>=f.minLength; setFieldState(input,valid,f.message); if(!valid)ok=false;}); if(!validateDateRange(form))ok=false; if(!ok){const first=form.querySelector('.error'); if(first)first.focus(); return;} showMessageOptions(form,buildContactMessage(form),'Your inquiry is ready to send');}); form.querySelectorAll('.form-input,.form-select,.form-textarea').forEach(input=>input.addEventListener('input',()=>{input.classList.remove('error','success'); const error=input.closest('.form-group')?.querySelector('.form-error'); if(error)error.remove();}));}
function initAvailabilityForm(){const form=document.getElementById('availability-form'); if(!form)return; form.addEventListener('submit',e=>{e.preventDefault(); clearValidation(form); let ok=true; ['checkin','checkout','guests','room'].forEach(name=>{const input=form.querySelector(`[name="${name}"]`); const valid=!!input?.value; setFieldState(input,valid,'This field is required'); if(!valid)ok=false;}); if(!validateDateRange(form))ok=false; if(!ok){const first=form.querySelector('.error'); if(first)first.focus(); return;} window.open(buildWhatsAppUrl(buildAvailabilityMessage(form)),'_blank','noopener,noreferrer');});}
function validateDateRange(form){const checkin=form.querySelector('[name="checkin"]'); const checkout=form.querySelector('[name="checkout"]'); if(!checkin||!checkout||!checkin.value||!checkout.value)return true; if(new Date(checkout.value)<=new Date(checkin.value)){checkout.classList.remove('success'); checkout.classList.add('error'); showError(checkout,'Check-out date must be after check-in date'); return false;} return true;}
function setFieldState(input,valid,message){if(!input)return; input.classList.remove('error','success'); if(!valid){input.classList.add('error'); showError(input,message);} else input.classList.add('success');}
function clearValidation(form){form.querySelectorAll('.form-error').forEach(el=>el.remove()); form.querySelectorAll('.error,.success').forEach(el=>el.classList.remove('error','success')); form.parentNode.querySelectorAll('.form-success').forEach(el=>el.remove());}
function getFormValue(form,name,fallback='-'){return form.querySelector(`[name="${name}"]`)?.value?.trim()||fallback;}
function buildAvailabilityMessage(form){return ['Hello Hotel Canbo, I would like to check room availability.',`Check-in: ${getFormValue(form,'checkin')}`,`Check-out: ${getFormValue(form,'checkout')}`,`Guests: ${getFormValue(form,'guests')}`,`Room type: ${getFormValue(form,'room')}`].join('\n');}
function buildContactMessage(form){return ['Hello Hotel Canbo, I have an inquiry.',`Name: ${getFormValue(form,'name')}`,`Email: ${getFormValue(form,'email')}`,`Phone: ${getFormValue(form,'phone')}`,`Inquiry type: ${getFormValue(form,'inquiry')}`,`Check-in: ${getFormValue(form,'checkin')}`,`Check-out: ${getFormValue(form,'checkout')}`,`Guests: ${getFormValue(form,'guests')}`,`Message: ${getFormValue(form,'message')}`].join('\n');}
function buildWhatsAppUrl(message){return `https://wa.me/${HOTEL_PHONE_INTERNATIONAL}?text=${encodeURIComponent(message)}`;}
function buildEmailUrl(message){return `mailto:${HOTEL_EMAIL}?subject=${encodeURIComponent('Hotel Canbo Inquiry')}&body=${encodeURIComponent(message)}`;}
function showError(input,message){const parent=input.closest('.form-group')||input.parentNode; let error=parent.querySelector('.form-error'); if(!error){error=document.createElement('span'); error.className='form-error'; parent.appendChild(error);} error.textContent=message;}
function showMessageOptions(form,message,title){const div=document.createElement('div'); div.className='form-success'; div.innerHTML=`<svg class="success-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg><h3>${title}</h3><p>This website is static, so the message will be sent through WhatsApp or your email app.</p><div class="btn-group" style="justify-content:center;margin-top:18px;"><a class="btn btn-whatsapp" target="_blank" rel="noopener noreferrer" href="${buildWhatsAppUrl(message)}">Send via WhatsApp</a><a class="btn btn-secondary" href="${buildEmailUrl(message)}">Send via Email</a><button type="button" class="btn btn-text" id="reset-form">Edit Message</button></div>`; form.style.display='none'; form.parentNode.appendChild(div); div.querySelector('#reset-form')?.addEventListener('click',()=>{div.remove(); form.style.display=''; form.querySelector('input,select,textarea')?.focus();});}
function setMinimumDates(){const today=new Date().toISOString().split('T')[0]; document.querySelectorAll('input[type="date"][name="checkin"],input[type="date"][name="checkout"]').forEach(input=>{input.min=today;});}
function updateCurrentYear(){document.querySelectorAll('#current-year').forEach(el=>{el.textContent=String(new Date().getFullYear());});}
